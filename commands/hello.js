const Discord = require("discord.js")

module.exports.run = async (bot, message, args) => {
    if(cmd === `${prefix}hello`){
        return message.channel.send(`Hello ${author.tag}`);
      
      }
}

module.exports.help = {
    name:"hello"
}